// Data Types in JavaScript

// use the keywords
// "String", "Number", "Boolean", "Null", "Undefined", "Symbol", "Object"

// Yes the keywords start with capital letters

let score="33dd";
 let convertedScore=Number(score);

 console.log(convertedScore); // NaN

 //Although the value of score is a string, it gets be converted to a number but not exaxctly
 //because it has some characters in it. Hence it returns NaN (Not a Number)

 //Data type of Nan is number

 console.log(typeof 3n); // bigint